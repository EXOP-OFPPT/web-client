// import { useSelector } from "react-redux";
// import { RootState } from "@/state/store";
// import KpiCard from "./TaskCard";

export default function DisplayTasksCards() {
  // const data = useSelector((state: RootState) => state.getKpis.kpis);

  return (
    // <div className="px-4 grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-4">
    //   {data.map((item, index) => {
    //     return <KpiCard key={index} data={item} />;
    //   })}
    // </div>
    <h1 className="h-full w-full flex justify-center items-center text-2xl">This View Is not ready yet ):</h1>
  );
}
